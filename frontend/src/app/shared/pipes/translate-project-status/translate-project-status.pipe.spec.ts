import { TranslateProjectStatusPipe } from '../translate-project-status.pipe';

describe('TranslateProjectStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateProjectStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
