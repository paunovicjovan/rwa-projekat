import { TranslateRolePipe } from './translate-role.pipe';

describe('TranslateRolePipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateRolePipe();
    expect(pipe).toBeTruthy();
  });
});
